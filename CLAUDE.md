# gal.tidhar.org.il — repo notes

This repo is **Gal Tidhar's personal portfolio** site.

- **Deploys to:** `gal.tidhar.org.il` via GitHub Pages (`main` branch, root, served from `index.html`, CNAME `gal.tidhar.org.il`).
- **GitHub:** `tatarco/gal-tidhar`
- **Personal WhatsApp:** `+972503843427`. The `wa.me/` links here use it (hero link-row + contact list).

## Sibling repo — the business site

The **ZaZet Solutions** business site (`zazet-solutions.hr`) is a **SEPARATE repo: `tatarco/zazet-solutions`** (its own GitHub Pages deploy, served from that repo's `index.html`, CNAME `zazet-solutions.hr`). The two sites are often worked on in parallel.

- That site's WhatsApp is the **business** number `+385991904995` (Croatian) — different from this site's personal number. Don't cross the two numbers between sites.
- To change the business site, edit `tatarco/zazet-solutions/index.html` and push there. Changes here do **not** affect it.

⚠️ Note: the `zazet-solutions` working dir contains a file `_gal_tidhar_index.html`, which is a **stale copy** of THIS site. It is a decoy — this repo is the source of truth for `gal.tidhar.org.il`.
